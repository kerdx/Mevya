#!/bin/sh

set -eu

# Fedora's liveinst launcher handles the required privilege escalation and
# Wayland environment setup itself. Do not wait for a guessed window id here:
# that could leave the desktop launcher blocked before Anaconda is shown.
exec /usr/bin/liveinst "$@"
